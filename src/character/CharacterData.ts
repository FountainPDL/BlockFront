export interface CharacterData {
  skin:string;
  helmet:string;
  backpack:string;
  emote:string;
}

export const DEFAULT_CHARACTER:CharacterData = {
  skin:"assault",
  helmet:"default",
  backpack:"default",
  emote:"wave"
};
